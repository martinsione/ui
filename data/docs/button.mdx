---
title: Button
description: A control that triggers an action. Button labels should express what action will occur when the user interacts with it.
---

## Sizes

<div className="flex items-center gap-8">
  <Button size="sm">Button</Button>
  <Button size="md">Button</Button>
  <Button size="lg">Button</Button>
</div>

---

## Shapes

<div className="flex items-center gap-8">
  <Button shape="square" size="sm">
    <ArrowUp />
  </Button>
  <Button shape="square" size="md">
    <ArrowUp />
  </Button>
  <Button shape="square" size="lg">
    <ArrowUp />
  </Button>

  <Button shape="circle" size="sm">
    <ArrowUp />
  </Button>
  <Button shape="circle" size="md">
    <ArrowUp />
  </Button>
  <Button shape="circle" size="lg">
    <ArrowUp />
  </Button>
</div>

---

## Prefix and Suffix

<div className="flex items-center gap-8">
  <Button prefix={<ArrowUp />}>Upload</Button>
  <Button suffix={<ArrowUp />}>Upload</Button>
  <Button prefix={<ArrowUp />} suffix={<ArrowUp />}>
    Upload
  </Button>
</div>

---

## Colors

<div className="flex items-center gap-8">
  <Button color="secondary">Upload</Button>
  <Button>Upload</Button>
  <Button color="error">Upload</Button>
  <Button color="warning">Upload</Button>
  <Button color="alert">Upload</Button>
  <Button color="violet">Upload</Button>
</div>

---

## Variant - Ghost

<div className="flex items-center gap-8">
  <Button variant="ghost" color="secondary">
    Upload
  </Button>
  <Button variant="ghost">Upload</Button>
  <Button variant="ghost" color="error">
    Upload
  </Button>
  <Button variant="ghost" color="warning">
    Upload
  </Button>
  <Button variant="ghost" color="alert">
    Upload
  </Button>
  <Button variant="ghost" color="violet">
    Upload
  </Button>
</div>

---

## Loading

<div className="flex items-center gap-8">
  <Button isLoading size="sm">
    Button
  </Button>
  <Button isLoading size="md">
    Button
  </Button>
  <Button isLoading size="lg">
    Button
  </Button>
</div>

---

## Disabled

<div className="flex items-center gap-8">
  <Button isDisabled size="sm">
    Button
  </Button>
  <Button isDisabled size="md">
    Button
  </Button>
  <Button isDisabled size="lg">
    Button
  </Button>
</div>
